package character;

public class Spring extends AnimationGameObject{

    public Spring(int x, int y, int drawWidth, int drawHeight, int imageWidth, int imageHeight, String imageName) {
        super(x, y, drawWidth, drawHeight, imageWidth, imageHeight, imageName);
    }
}
